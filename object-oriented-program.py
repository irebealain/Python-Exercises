# 1. **Bank Account:**
#    - Create a `BankAccount` class with methods to deposit, withdraw, and check the balance.
#    Ensure that the balance cannot go negative.
#
# 2. **Library System:**
#    - Create classes for `Book` and `Library`.
#    The `Library` class should be able to add books, remove books, and list all books.
#    Each `Book` should have attributes like title, author, and ISBN.
#
# 3. **Student Management:**
#    - Create a `Student` class with attributes like name, age, and grades.
#    Implement methods to add grades, calculate the average grade, and display student details.
#
# 4. **Shapes:**
#    - Create a base class called `Shape` with a method to calculate the area.
#    Then, create subclasses `Circle`, `Square`, and `Rectangle` that override
#    the area method to calculate the area for each specific shape.
